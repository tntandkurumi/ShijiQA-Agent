# 2026-06-01 开发日志

## 今日目标

- 进入阶段 3：搭建 `wenyuan-web` React + TypeScript + Vite 前端基础工程。
- 本轮只实现前端基础页面、路由、API 客户端和认证状态，不进入复杂对话页细节。
- 继续进入阶段 4：实现完整对话页、会话列表、消息历史、SSE 流式回答和过程块折叠。
- 继续阶段 5 的一个小目标：补历史过程块和检索块回放能力。
- 继续阶段 5 小目标：支持点击某条助手回答，切换查看该轮问答的过程块。
- 继续阶段 6 小目标：接入可选真实 OpenAI-compatible LLM，并保持模拟兜底。
- 继续阶段 5/6 收尾：补启动脚本、浏览器验收清单和真实 LLM 配置说明。
- 继续阶段 5/6 收尾：补自动化冒烟测试脚本，覆盖后端核心链路和前端构建。
- 继续优化：真实 Agent 行进过程由真实 LLM API 驱动，过程可视化内嵌到助手消息中。

## 完成事项

- 确认 `wenyuan-web` 初始目录为空骨架，仅有 `src/components`、`src/pages`、`src/lib`。
- 确认本机 Node.js `v22.14.0`、npm `10.9.2` 可用。
- 手工创建 Vite + React + TypeScript 工程基础文件。
- 实现 API 客户端，默认连接 `http://127.0.0.1:8000`。
- 实现认证上下文，支持登录、注册、退出和 token 本地保存。
- 实现网站介绍页、登录页、注册页、受保护问答占位页。
- 实现路由、受保护路由和站点外壳。
- 实现底部文言名句轮滚。
- 实现古风纯文本 CSS 视觉风格。
- 新增 `wenyuan-web/README.md` 与 `.gitignore`。
- 完成阶段 4 前端对话页：
  - 扩展 API 客户端，支持会话、消息和 POST SSE。
  - 实现完整问答页，包含会话列表、消息历史、模型选择、发送框。
  - 实现 SSE 流式答案增量展示。
  - 实现过程块和检索知识块折叠展示。
  - 实现三栏桌面布局与移动端响应式布局。
- 完成阶段 5 小目标：
  - 后端新增 `GET /api/conversations/{id}/agent-runs`。
  - 该接口返回 Agent 运行记录、过程块和检索知识块。
  - 前端切换会话或刷新后会自动加载最近一次过程块和检索块。
- 完成阶段 5 第二个小目标：
  - 后端 `agent-runs` 响应新增 `assistant_message_id`。
  - 前端支持点击某条助手回答，右侧切换到该轮问答对应的过程块和检索块。
  - 当前选中的助手回答会有视觉高亮。
- 完成阶段 6 小目标：
  - 后端 Agent 服务新增 OpenAI-compatible 真实模型调用。
  - 未配置 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL` 时自动使用模拟 Agent。
  - 真实模型调用失败时自动降级模拟 Agent。
  - 问答过程块新增 `llm_status`，用于说明真实/模拟/降级状态。
- 完成本轮进一步优化：
  - 后端真实 Agent 改为 `plan -> tool_calls -> observations -> final_answer`。
  - 无真实 LLM 配置时不再伪装思考/行动过程，只提示未配置并展示模拟检索数据。
  - 模拟检索工具封装为 `search_person`、`search_kg`、`search_bilingual` 等工具接口。
  - 模型种子配置补充 Kimi、豆包、MiMo、DeepSeek、通义/百炼等 OpenAI-compatible 模型名和 base_url，不写入任何 API Key。
  - 前端移除独立右侧过程模块，改为每条助手消息内部展示过程块和知识块。
  - 前端新增复制、重新生成、停止生成、会话搜索。
  - 新增 `docs/08_springboot_decision.md`，说明当前不建议加入 Spring Boot。
- 补齐用户反馈问题：
  - 后端新增 `DELETE /api/conversations/{id}`。
  - 前端会话列表新增删除按钮。
  - 冒烟测试新增删除会话验证。
  - 明确选择 DeepSeek V4 Pro 只选择模型配置，仍需要真实 API Key 才能调用真实 Agent。
- 完成本机真实模型配置接入：
  - 后端启动时自动读取 `wenyuan-api/.env.local` 和 `.env`。
  - 后端 `.gitignore` 已忽略 `.env.local`，避免本机密钥进入仓库。
  - 已从 `test3.ipynb` 提取 DeepSeek V4 Pro 的本机配置写入 `.env.local`，未在文档或日志记录密钥内容。
  - 配置读取兼容 Windows UTF-8 BOM，避免首个环境变量解析失败。
- 完成本机多模型真实 API 配置接入：
  - 新增 `wenyuan-api/.llm.models.local.json` 本机多模型密钥配置格式。
  - 后端按前端所选模型名优先读取对应 `api_key/base_url/model`，不再只依赖全局 DeepSeek 配置。
  - 已从 `test3.ipynb` 提取 Kimi、豆包、MiMo、DeepSeek V4 Pro、DeepSeek V4 Flash、Qwen、GLM 的本机配置，未在文档或日志记录密钥内容。
  - 后端 `.gitignore` 已忽略 `.llm.models.local.json`。
- 按 `test3.ipynb` 优化真实 Agent 问答流程：
  - 后端真实模型调用改为 OpenAI-compatible `tools` + `tool_choice=auto`。
  - 系统提示词改为《陈书》与南朝历史高级研究助手规范。
  - 工具调用最多 5 轮，工具由真实模型自主选择，后端执行模拟检索工具。
  - 若未得到最终答案，追加强制回答提示生成最终答案。
  - 过程可视化移除 `思考摘要`，只展示行动、工具调用、工具返回和最终答案。
  - 多轮会话会带入最近 8 条用户/助手历史消息。
- 修正模拟检索返回边界：
  - 真实模型未调用工具时，不再补充任何模拟检索知识块。
  - 模拟知识库增加最低命中阈值，低相关问题不返回虚拟数据。
  - 工具被调用后只返回对应库命中的模拟数据，不再无条件兜底到其他库。
  - 系统提示词补充身份、寒暄、使用说明等非史籍问题不调用工具。
- 重构模拟数据库实现：
  - 新增 `prepared_retrieval.py`，按 8 个数据库分别准备预置数据。
  - 人物、诗文、官职、地理、典故事件、术语、知识图谱调用根目录检索脚本的 `retrieve` 接口形状。
  - 官职、地理、典故事件、术语使用模拟 Embedding/Collection，保持“精确匹配优先，否则向量距离分组”的流程。
  - 双语数据库按稠密检索、BM25 稀疏检索和 RRF 融合流程做本机模拟。
  - Agent 工具执行层改为只在真实模型 tool calling 后调用对应预置库。
  - 新增 `docs/09_prepared_mock_databases.md` 记录 8 个库、工具名和对应检索脚本。
- 调整前端 Agent 过程展示：
  - 后端新增 `thinking` 公开决策日志块，展示模型进入检索或直接回答的可视化思考过程。
  - 前端助手消息改为先展示过程块和检索依据，再展示最终答案。
  - 新过程顺序为 `thinking -> action -> tool_call -> observation -> 最终答案`。
- 完成阶段 5/6 收尾基础设施：
  - 新增 `scripts/start_backend.ps1`。
  - 新增 `scripts/start_frontend.ps1`。
  - 新增 `scripts/start_dev.ps1`。
  - 新增 `scripts/smoke_test.ps1`。
  - 新增浏览器验收清单 `docs/06_browser_acceptance.md`。
  - 新增真实 LLM 配置说明 `docs/07_llm_configuration.md`。
  - 更新 docs 索引、项目工作指引和前后端 README。

## 验证结果

- 已安装前端依赖，生成 `package-lock.json`。
- 首次 `npm install` 因命令超时返回 `124`，但输出显示依赖安装完成且 0 漏洞；后续 `npm ls --depth=0` 确认依赖存在。
- 首次构建因 TypeScript 6 弃用 `moduleResolution: "Node"` 失败，已改为 `Bundler`。
- 第二次构建因缺少 React 类型声明失败，已安装 `@types/react`、`@types/react-dom`。
- 已运行 `npm run build`，构建通过。
- 后端与前端均用前台短跑方式确认可启动。
- 当前工具环境中 `Start-Process` 后台启动服务会立即退出，未留下常驻进程。
- 阶段 4 修改后再次运行 `npm run build`，构建通过。
- 使用临时 PowerShell Job 同时启动后端和前端，验证 `/api/health` 返回 `ok`，前端首页返回 HTTP 200。
- 阶段 5 修改后运行后端 `compileall` 和 `app.main` 导入检查，均通过。
- 阶段 5 修改后运行前端 `npm run build`，构建通过。
- 已通过临时后端 Job 验证一次问答后，`/api/conversations/{id}/agent-runs` 返回 1 条运行记录、4 个过程块、5 个检索块。
- 第二个阶段 5 小目标后再次运行后端 `compileall`、`app.main` 导入检查和前端 `npm run build`，均通过。
- 已验证 `agent-runs` 返回 `assistant_message_id`，并保留 4 个过程块、5 个检索块。
- 阶段 6 小目标后运行后端 `compileall` 通过。
- 已直接调用 `build_agent_result` 验证无密钥时 `used_real_llm=False`，过程块数量为 5，最后一块为 `llm_status`。
- 已通过临时后端 Job 验证 `/api/chat/stream` 在无密钥时仍返回 `done`，并包含 `llm_status` 与模拟提示。
- 已用 PowerShell parser 检查 `start_backend.ps1`、`start_frontend.ps1`、`start_dev.ps1` 语法。
- 已再次验证后端 `app.main` 可导入。
- 已再次运行前端 `npm run build`，构建通过。
- 已运行 `.\scripts\smoke_test.ps1 -SkipFrontendBuild`，后端核心链路通过。
- 已运行完整 `.\scripts\smoke_test.ps1`，后端核心链路和前端构建均通过。
- 修正 PowerShell 5 对 `.ps1` 中中文路径/字符串的编码解析问题，脚本内部改为 ASCII 输出并自动扫描已有 Python 解释器。
- 删除会话功能补齐后，再次运行后端 `compileall`、前端 `npm run build` 和完整 `smoke_test`，均通过。
- 已验证后端能读取 `.env.local` 中的 DeepSeek V4 Pro 配置，`llm_enabled=True`，且密钥存在性为 `True`。
- 已再次运行后端 `compileall` 和 `app.main` 导入检查，均通过。
- 已验证 7 个模型配置均可被后端识别，且各自 `enabled=True`。
- 已再次运行后端 `compileall` 和 `app.main` 导入检查，均通过。
- 已验证 7 个模型的 thinking/reasoning 参数策略与 `test3.ipynb` 保持一致。
- 已运行后端 `compileall`、后端导入检查和前端 `npm run build`，均通过。
- 已验证 `你是谁` 不命中模拟知识库，直接工具调用也返回空；`陈霸先是谁` 能正常命中人物库。
- 已再次运行后端 `compileall`、后端导入检查和前端 `npm run build`，均通过。
- 已验证 8 个工具均可按对应数据库返回预置数据和原脚本同款格式。
- 已验证未配置模型时检索块为 0；模拟真实模型调用 `search_person` 后才产生人物数据库检索块。
- 已再次运行后端 `compileall`、后端导入检查和前端 `npm run build`，均通过。
- 已验证模拟工具调用链过程顺序为 `thinking, action, tool_call, observation`，最终答案单独展示在过程之后。
- 已再次运行后端 `compileall`、后端导入检查和前端 `npm run build`，均通过。

## 待办事项

- 创建前端工程文件。
- 实现登录、注册、介绍页。
- 实现 API 客户端、token 保存和受保护路由。
- 完成一次前端构建或静态检查。
- 下一步进入阶段 4：实现完整对话页、会话列表、消息历史、SSE 流式回答和过程块折叠。
- 本轮阶段 4 先做前端对接后端现有接口，不启用 Figma/Canva 生成设计稿。
- 下一步继续阶段 5：浏览器人工验收、移动端/宽屏视觉修正、真实 LLM 配置说明。
- 真实 LLM 调用已接入；后续需要在实际密钥环境中做一次真实供应商联调。
- 后续人工验收按 `docs/06_browser_acceptance.md` 执行。
- 本轮优先解决“刷新后历史消息保留但右侧过程块不回放”的问题。
- 本轮优先解决“历史会话中多轮问答只能默认展示最近一次过程”的问题。
- 本轮优先解决“模型选择已有真实模型项，但后端仍只使用模拟答案”的问题。
- 本轮优先解决“演示启动和验收步骤依赖口头说明”的问题。
- 本轮优先解决“每次修改后缺少一键核心链路验证”的问题。

## 风险与阻塞

- 后端需要先启动在 `http://127.0.0.1:8000`，前端接口才能真实联调。
- 当前未保持前后端常驻运行；需要用户在终端中手动启动，或后续补专用启动脚本。
- 历史过程块回放接口已补齐，并已支持点击助手回答切换对应过程；后续需要做浏览器人工验收。
- 真实 LLM 联调需要用户提供本地环境变量，不应写入仓库。
- 当前本机已写入多模型真实 API 配置；如果已有后端进程正在运行，需要重启后端才能加载新配置。
