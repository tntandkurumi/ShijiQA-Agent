# 2026-05-31 开发日志

## 今日目标

- 建立项目开发节奏和文档管理基线。
- 暂停一次性大规模实现，改为稳定分阶段推进。
- 正式进入阶段 1：实现后端最小可运行基线。
- 继续进入阶段 2：实现后端会话接口、模拟知识库和 SSE 流式问答。

## 完成事项

- 确认前一轮中断后仅创建了空的 `wenyuan-api/`、`wenyuan-web/` 骨架。
- 创建 `docs/` 文档目录。
- 创建 `dev-logs/` 每日开发日志目录。
- 创建 `scripts/` 开发辅助脚本目录。
- 编写项目需求、技术架构、前端设计、开发步骤、质量安全规范。
- 明确第一阶段仍以模拟知识库和可运行演示原型为目标。
- 完成 `wenyuan-api` 后端最小基线：
  - 新增 FastAPI 应用入口。
  - 新增配置、数据库连接、SQLAlchemy 表模型。
  - 新增注册、登录、当前用户接口。
  - 新增模型列表、文言名句、健康检查接口。
  - 新增 SQLite 兜底和 MySQL 环境变量配置说明。
  - 新增后端 README、依赖清单和 `.env.example`。
- 曾创建并安装 `wenyuan-api/.venv` 后端依赖；后按用户要求删除该环境，改用已有解释器。
- 修正 Windows + Python 3.9 环境下 `greenlet` 源码编译失败问题，将 `greenlet` 固定为 `3.1.1`。
- 修正 Python 3.9 不兼容 `str | None` 类型写法的问题。
- 完成阶段 2 后端基线：
  - 新增会话列表、创建会话、会话消息接口。
  - 新增模拟八类知识库。
  - 新增模拟 Agent 过程块生成。
  - 新增 `/api/chat/stream` SSE 流式问答接口。
  - 将用户消息、助手回答、Agent 过程日志、检索知识块写入数据库。

## 验证结果

- 已通过目录读取确认当前工程状态。
- 已创建文档与日志文件，未继续写入业务功能代码。
- 已运行 `python -m compileall app`，后端 Python 文件语法检查通过。
- 当前系统 Python 缺少 `fastapi` 依赖。
- 曾创建 `wenyuan-api/.venv` 虚拟环境；已删除。
- 依赖安装命令需要联网授权，用户已拒绝授权，因此本轮未执行启动验证。
- 已确认本地 Python 3.11 虚拟环境可用，且已安装 `fastapi`、`sqlalchemy`、`uvicorn`、`pymysql`。
- 已确认 `wenyuan-api/.venv` 删除成功。
- 已使用现有解释器重新运行 `compileall` 和 `app.main` 导入检查，均通过。
- 已使用现有解释器启动临时 Uvicorn Job 并验证 `/api/health` 返回正常。
- 已执行数据库初始化，SQLite 兜底成功创建表并写入 3 条默认模型配置。
- 已通过 PowerShell Job 启动 Uvicorn 并验证 `/api/health` 返回 `status=ok`、`database=sqlite`。
- 已验证注册、登录、鉴权访问 `/api/models`，返回模型数量为 3。
- 已验证创建会话、SSE 问答、读取会话消息。
- SSE 返回内容包含 `done` 与 `retrieval_chunk`。
- 数据库中已写入 `agent_runs`、`retrieval_chunks`、`messages` 记录。

## 待办事项

- 下一步从“阶段 1：后端最小可运行”开始。
- 先实现 FastAPI 配置、数据库连接和认证接口。
- 后端稳定后再进入前端页面实现。
- 本轮优先完成 `wenyuan-api` 的配置、数据库、注册登录、模型列表和名句接口。
- 后续如需安装依赖，运行 `.\.venv\Scripts\python.exe -m pip install -r requirements.txt`。
- 后续启动后端，运行 `.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000`。
- 下一阶段进入会话接口、模拟 Agent 与 SSE 之前，先完成后端启动验证。
- 阶段 1 后端最小基线已完成；下一步进入阶段 2：会话接口、模拟知识库、Agent 过程块与 SSE。
- 本轮阶段 2 控制范围：只做后端会话、消息、模拟 Agent 和 SSE；暂不做前端。
- 阶段 2 后端基线已完成；下一步进入阶段 3：React + TypeScript + Vite 前端基础页面。

## 风险与阻塞

- 当前尚未安装前后端依赖。
- MySQL 是否可用未知，后续实现必须保留 SQLite 兜底。
- 真实 LLM API Key 不应写入代码，需要通过环境变量配置。
- 后台长期启动方式仍需后续统一脚本化；本轮验证使用临时 Job 启停，未留下常驻服务。
- PowerShell 旧版 `Invoke-WebRequest` 读取 SSE 时需要 `-UseBasicParsing`。
