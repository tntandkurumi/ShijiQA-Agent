# 2026-06-02 开发日志

## 今日目标

- 生成面向 GitHub 展示的独立项目副本 `github-ShijiQA-Agent`。
- 将根目录散落的论文实验、检索和向量生成脚本整理进 `research_code/`。
- 清理本机密钥、构建产物和运行缓存，保证仓库适合公开前检查。

## 完成事项

- 新建 `github-ShijiQA-Agent` 发布副本目录，未移动或破坏原始项目目录。
- 复制前后端、文档、启动脚本和开发日志，排除 `.env.local`、`.llm.models.local.json`、数据库、日志、`node_modules` 和 `dist`。
- 新增 `research_code/retrieval`、`research_code/preprocessing`、`research_code/experiments`。
- 将 8 类检索脚本归档到 `research_code/retrieval`。
- 将向量生成和 BM25 构建脚本归档到 `research_code/preprocessing`。
- 将 `agent.py`、`main.py`、脱敏后的 `test3.ipynb` 归档到 `research_code/experiments`。
- 后端 `prepared_retrieval.py` 改为从 `research_code/retrieval` 加载检索脚本。
- `agent.py` 移除硬编码 API Key，改为从环境变量读取。
- `agent.py` 和实验 `main.py` 增加归档后检索脚本路径适配。
- 新增根目录 `README.md`、根目录 `.gitignore` 和 `research_code/README.md`。
- 更新后端 README、docs 索引和预置数据库说明。
- 调整 GitHub 版本 `smoke_test.ps1`，不再要求模拟模型主动返回检索块，改为直接验证 8 个预置检索工具。

## 验证结果

- 已确认 GitHub 副本中没有 `node_modules`、`dist`、`.env.local`、`.llm.models.local.json`、SQLite 数据库和运行日志。
- 已运行真实密钥格式扫描，未发现 `sk-` 或 UUID 样式密钥。
- 已运行后端 `compileall` 和 `app.main` 导入检查，均通过。
- 已运行前端 `npm install --cache ..\.npm-cache`，0 漏洞；随后 `npm run build` 通过。
- 已修正并运行完整 `scripts/smoke_test.ps1`，后端链路、8 个预置检索工具和前端构建均通过。
- 已清理验证过程中生成的 `node_modules`、`dist`、`.npm-cache`、`__pycache__` 和 SQLite 运行库。
- 已将脱敏 notebook 的单元格 ID 规范化为普通 `cell-xxx`。
- 已按论文最终稿中的数据表和工具表重写根目录 `README.md`：
  - 移除“项目亮点”“技术栈”等目的性较强的小标题。
  - 增加数据组织、8 类检索工具、输入输出、检索算法和 Agentic 工作流说明。
  - 在末尾说明真实数据集、真实向量库和 BM25 索引暂未开源。
- 已在 README 各部分补充前后端工程内容：
  - 简介中说明 React + TypeScript + Vite 前端和 FastAPI + SQLAlchemy + SSE 后端。
  - 系统结构、数据组织、检索工具、Agentic 工作流、本地运行、模型配置和验证部分均补充前后端职责。
  - 保持不单独设置“技术栈”标题，避免表达目的性过强。

## 待办事项

- 最终确认 `git init` 前的文件清单。

## 风险与阻塞

- 当前还没有上传 GitHub，也没有初始化 git。
- `research_code` 中预处理脚本仍依赖论文阶段的真实数据路径，GitHub 版本仅作为研究代码归档和工程展示。
